# demobook-server

## Setup

```sh
$ git clone git@github.com:hiroppy/demobook.git
$ cd demobook/server
$ cp .env.sample .env # and edit
$ docker-compose up -d
$ open https://localhost:3000
```

## API

### /health

### / demos
